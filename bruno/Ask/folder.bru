meta {
  name: Ask
  seq: 3
}

docs {
  The full RAG flow: embed the query, check the semantic cache, retrieve
  context, build a grounded prompt, generate an answer with the LLM
  provider, and validate citations before returning a response.
}
