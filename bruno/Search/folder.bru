meta {
  name: Search
  seq: 2
}

docs {
  Raw semantic retrieval over previously ingested content, without the LLM
  generation, prompt building, citation validation, or caching layers that
  `/ask` applies on top. Useful for inspecting what the retrieval step would
  return for a given query.
}
